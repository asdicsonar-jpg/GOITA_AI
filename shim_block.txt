  if (typeof window !== "undefined") {
    window.__T = {
      app, G, G_B, $, modal, closeModal,
      startGame, humanTurn, onHandTap, renderHandTray, renderAll, getRec, recPattern,
      seatTag, SEAT_JA, GLOSSARY, GLOSSARY_SKIP_AUTOLINK,
      seatDisp, teamDisp, boardSummaryText, showYakuSplash,
      dealAnimAllowed, dealAnimStart, dealCleanup, dealSeatOrder, dealDelayMs, FX, fx,
      kirikoBurn, kirikoRelease, showWinCut, kirikoTier, kirikoTierCapped,
      endShiCounts, computeSignals, auditOuShiSignal, askHumanGoshi, detectSignal, signalTruth,
      renderReviewList, openReviewOn, openReview, computeCoach,
      rebuildState, branchRecBase, applyAndLog, onGameOver,
      resumeSavedMatch, resumeFromMove, loadMatchProgress, saveMatchProgress, deleteMatchProgress, normalizeRec,
      readSetup, enterGame, saveCareerToStorage, loadCareerFromStorage, matchSaveAllowed, saveCfgToStorage,
      scaffoldStamp, scaffoldStampAllowed, renderKeikoPanel,
      SCAFFOLDS, gradLoad, gradSave, gradMasteryFor, gradThresholdFor, gradPropose,
      gradTrialStart, gradDismiss, gradOnGameFinished, gradTrialRevert, gradTrialContinue,
      openCareer, gradLedgerHtml, fmtMonthDay,
      SRS_STORAGE_KEY, SRS_STEPS, SRS_MAX_CARDS, SRS_BUDGET_BYTES, CAREER_STORAGE_KEY,
      addDays, daysBetween, srsLoad, srsSlim, srsAddPuzzle, srsAddJoseki, srsGrade,
      TOURWIN_STORAGE_KEY, tourWinLoad, tourWinRecord, readinessChecklistHtml, openReadinessChecklist,
      showMatchEnd, scAllOff, buildTempoStats, tempoHtml,
      openJoseki, JOSEKI, WHY2JK,
      slimRecForStorage, TUT, CONCEPT2TUT, DRILLS,
      // v91a(A-1〜A-5)テスト用に追加公開(tests/側コピーのみ・本番には非公開):
      syncSetupFormFromCfg, stopToSetup, newMatch,
      srsReviewStart, srsReviewNext, srsDue, today,
      openPastSelf, pastMaybeAdd, PAST_STORAGE_KEY,
      keikoDismissToday, keikoRenderCard, KEIKO_STORAGE_KEY,
      // v92a(M-1〜M-9,M-14)テスト用に追加公開(tests/側コピーのみ・本番には非公開):
      auditOppProfile, applyOppProfile, setOppProfileBoth, oppCountsMigrateBl, auditTblConformity,
      buildConceptProfile, CONCEPTS, conceptOf, conceptMastery, conceptDiag,
      srsAddPuzzle, srsReviewPuzzle, srsReviewShowDone, srsSaveStore, srsEvictionOrder,
      advProbe, generatePuzzle, runPuzzleGen, openPuzzle, PUZZLE_BANDS,
      startPractice, endPractice, onPracticeOver, ensureGameScreen,
      clarityAnalyze, mcReevaluate, mcReviewSeed, MC_REVIEW_DETS, candidateRanking,
      OUSIG_TRUST_READ, convTrustVal, lossOf, coachSummary, ensureAnalyzed,
      // v91b/v91c(B-1〜B-4,C-1)テスト用に追加公開(tests/側コピーのみ・本番には非公開):
      keikoProposeDue, keikoProposeGradCompute, keikoProposeGrad, keikoEvalGradAsync,
      pastLoad, pastSave, pastDue, PAST_DUE_DAYS, pastShowCompare,
      applyLadderPreset, LADDER_PRESETS, srsReviewJoseki, openUshitsuInfo,
      initMatchResumeUI, toast,
      // srsPersistGradedCardはv91bで新設した関数のため、旧バージョン(v91a/v92a等)のsrcとこのshimを
      // 組み合わせてbeforeフィクスチャを作るテスト(例: test_v92b_coach_mc.js)がReferenceErrorに
      // ならないよう、typeofガードで存在しない場合はundefinedにフォールバックする。
      srsPersistGradedCard: (typeof srsPersistGradedCard !== "undefined" ? srsPersistGradedCard : undefined),
    };
  }
