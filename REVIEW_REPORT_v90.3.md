# レビューレポート v90.3「かかり応え符丁監査誤判定の修正」

- Reviewer: Fable5（goita-dev-loop フェーズ3）
- 対象: `goita-app-v90.3.zip`（index.html 16,854行・CACHE_NAME v104）+ `IMPLEMENTATION_REPORT_v90.3.md`
- 計画: `PLAN_v90_3_gotae_audit.md`（本レビューと同一セッションで立案・原文照合済み）

## 総合判定

**承認**（デプロイ可。条件: 従来からのデプロイ前チェックリスト=実機確認+実ブラウザaxeの実施）

## レビューで実施した独立検証

1. **ファイル全体diff（v90.2→v90.3）**: 差分は計画の3箇所（buildコメント/detectSignalガード3行/computeSignalsのgotae認識15行）+`sw.js`のv104のみ、計41行。**便乗変更ゼロ**。エンジン領域に差分が無いことをファイル全体で機械確認したため、`G`/`G_B`のbyte-exact性は「v90.3＝v90.2（前回検証済み）＝v90b基準」の連鎖で成立する。レポートのsha256表と整合。
2. **同梱テスト5本の独立実行**: 当方環境でjsdomハーネスを再構築し、`test_f1`/`test_f2`/`test_f3`/`test_review_fixes`/`test_gotae`を実行。**全PASS**（Executor報告と一致）。
3. **報告局の実棋譜による裏取り（Executor未実施分の解消）**: 実装レポート§0の懸念（実棋譜#1〜#47が計画書に未添付で独自fixtureで代替）に対し、Sonar提供の実棋譜を当方が保持していたため、47手を完全再現する検証`test_real24.js`を新規作成して実行した。
   - 実棋譜47手が全手エンジン合法・手番一致で再生でき、実局どおり北の20点上がりで終局（＝棋譜・エンジン・fixtureの三者整合）
   - **v90.3**: #24（南・攻め香）→ `{type:"kakari_gotae", honest:true, text:"相方の「香」リード（かかり）に同駒で応えています。保有が相方に伝わる正しい符丁です。"}`
   - **v90.2（修正前・対照）**: 同一fixtureで `{type:"kyo2", honest:false, text:"...裏付けがありません。相方が期待してズレます。"}` — **スクリーンショットの警告文言と完全一致**
   - #29（南の伏せ直し後の銀攻め・攻め通し番号7=窓外）に新たな偽陽性が出ないことも確認
   - → 実装レポートの「機構的に等価な再現」という主張は実局で裏付けられた。既知の懸念点1は**解消**。

## 指摘事項

### Must-fix

なし。

### Nice-to-have

1. **`test_real24.js` を `tests/` に同梱する**（次コミットまたはデプロイ用パッケージング時で可）。実局の回帰テストとして価値があるため、当方が作成したものをそのまま引き渡す。実行結果は上記のとおり全PASS。

## 逸脱の裁定

1. **再現fixtureの独自構築（逸脱1）**: 承認。原因はExecutorではなく**計画書側の不備**（「Sonar提供のログをそのまま用いること」と書きながら棋譜データを計画書に埋め込まなかった、Planner=Fable5の落ち度）。Executorの「勝手に進めず逸脱として明記し、機構的等価性を修正前コードでの再現込みで実証する」という対応は模範的だった。プロセス教訓として記録: **計画書は自己完結にする（参照データは本文に埋め込む）**。
2. **text文言から「（かかり応え）」重複を除去（逸脱2）**: 承認。`sigHtml()`（14382行）が`jk`から`JOSEKI.kakari.name`を「（かかり応え）」チップとして自動追記することをコードで確認した。計画案のままなら二重表示になっており、是正は正当。既存kgb_pair/kyo2のtext実装（joseki名をtextに書かない）とも一貫する。

## 受け入れ基準の最終確認

| # | 基準 | 判定 |
|---|---|---|
| 1 | 報告局#24が肯定表示+チップ・赤警告なし | ✓（**実棋譜で当方確認**・修正前対照つき） |
| 2 | (b)(c)(e)(f)(g)全PASS（既存監査・NULLコントロール・し監査の不変） | ✓（独立実行で確認） |
| 3 | エンジンbyte-exact（sha256併記） | ✓（全体diffでも裏付け） |
| 4 | 既存4本+新規1本全PASS・node --check・禁止語彙0件 | ✓（テストは独立実行で確認） |
| 5 | CACHE_NAME v104・buildコメント | ✓ |
| 6 | tests/同梱 | ✓ |

## デプロイ前チェックリスト（残タスク・従来どおり）

- 実機確認: 報告局と同型の対局で#24相当が「かかり応え」表示になること／F-1配牌演出・F-2名札（v90.2からの持ち越し）
- 実ブラウザでのaxe-core 4テーマ再スキャン（F-2のaria-label広範変更の持ち越し確認）
- 研究モードの出題条件変化（応えへの偽刺激の除去）は計画どおりの仕様変更としてSonar了承済み扱い — 過去データと比較する際は条件非互換に留意
